"""Mark ``tools/`` as an importable Python package.

The map pipeline modules are importable as ``tools.map_pipeline.*``
when this `__init__.py` is present. The package itself contains only
sub-packages (e.g. ``tools.map_pipeline``, ``tools.content_validator``).
"""
